## Tracking

```code
git clone https://github.com/IIIIQIIII/test1-tracking.git
python -m colcon build --packages-select tracking
source install/setup.bash
ros2 launch tracking gazebo_tracking.launch.py
```